services:
  - type: web
    name: cinemaos-resolver
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cinemaos_resolver.py --serve
    envVars:
      - key: PORT
        value: 10000